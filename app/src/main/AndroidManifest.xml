<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 用于获取wifi的获取权限，wifi信息会用来进行网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <application
        android:allowBackup="true"
        android:name=".utils.MainApplication"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/wdfmsl"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.WearBus">
        <activity
            android:name=".activities.MainActivity"
            android:exported="false">
        </activity>
        <activity android:name=".activities.Bus"
            android:exported="false" />
        <activity android:name=".activities.Nearby"
            android:exported="false"
            tools:ignore="Instantiatable" />
        <activity android:name=".activities.SettingsActivity"
            android:exported="false"/>
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.guideSplashActivity"
            android:exported="false"/>
        <activity android:name=".activities.ad"
            android:exported="false"/>
        <activity android:name=".activities.exit"
            android:exported="false"/>
        <activity android:name=".activities.BusLineDetailActivity"
            android:label="线路详情"/>

        <activity
            android:name=".activities.NearbyStationsActivity"
            android:exported="false"
            android:label="附近站点" />

        <activity
            android:name=".activities.NearbyStationDetailActivity"
            android:exported="false"
            android:label="站点详情" />

        <activity
            android:name=".activities.SelectCityActivity"
            android:exported="false" />

        <activity
            android:name=".activities.WhereToActivity"
            android:exported="false"
            android:label="去哪里" />

        <activity
            android:name=".activities.AboutActivity"
            android:exported="false"
            android:label="关于" />

        <activity
            android:name=".activities.HiddenActivity"
            android:exported="false"
            android:label="隐藏界面" />
    </application>

</manifest>